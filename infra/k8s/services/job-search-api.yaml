apiVersion: v1
kind: Service
metadata:
  name: job-search-api
  namespace: job-market
spec:
  type: ClusterIP
  selector:
    app: job-search-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
